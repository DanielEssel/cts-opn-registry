import { PreRegistrationForm } from "@/app/components/pre-registration/PreRegistrationForm";

export const metadata = {
  title: "Rider Pre-Registration | PCRAA",
  description: "Register as a commercial rider in Greater Accra. Pay GHS 400 to secure your training slot.",
};

export default function PreRegistrationPage() {
  return <PreRegistrationForm />;
}